# Normalise line endings so container builds are identical regardless of the
# platform the checkout happened on.
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
